# Debug Changelog — 20260322_042509_415811

## Root Cause
Empty `.env` file → Supabase client failed to initialize → `getProductsPage` threw → App showed "Something went wrong" error.

## Changes Made

### 1. Populated `.env` with Supabase credentials
- **File:** `EZYPC-main/.env`
- **Change:** Copied `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` from `.env.example` into the empty `.env` file.
- **Revert:** Clear the contents of `.env` (restore to empty file).

## Investigated But Not Changed

### Orange viewport border
- **Finding:** The `rgba(255, 64, 0, 0.75) inset box-shadow` border visible in browser screenshots belongs to a `div#kombai-agent-overlay` element injected by the Kombai browser automation tool — **not** part of the application code. No fix needed.

## Revert Status
- [ ] Change 1 — Restore `.env` to empty
